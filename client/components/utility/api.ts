// Compatibility wrapper.
// Some older screens import the API client from "@/components/utility/api".
// The source of truth lives in "@/utils/api".
import api from "@/utils/api";

export default api;
